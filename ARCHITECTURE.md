# PLC System Process Flowchart\n\n`mermaid\nflowchart TD
\n    subgraph Initialization[初始化流程]\n        Start([开始：系统初始化])\n        Stop([结束：停止运行])\n        Start -->|检测 X1 = 1 ∧ X3 = 1| CheckOrigin{原点位置满足?}\n        CheckOrigin -->|是| AutoMode[进入自动模式]\n        CheckOrigin -->|否| ErrorStop([错误：未找到原点])\n        ErrorStop --> Stop\n    end\n\n    subgraph AutoMode[自动模式]\n        AutoStart[自动运行启动]\n        AutoStart --> Step1[机械手下降]\n        Step1 --> Delay1[延时模块：1秒]\n        Delay1 --> Step2[吸球]\n        Step2 --> Delay2[延时模块：1秒]\n        Delay2 --> Step3[机械手上升]\n        Step3 --> Step4[右行]\n        Step4 -->|到达目标| Step5[机械手下降]\n        Step5 --> Delay3[延时模块：1秒]\n    end\n
\n    subgraph HandMode[手动模式]\n        HandModeStart{手动模式?}\n        HandModeStart -->|是| ManualBranch[手动控制待命]\n        HandModeStart -->|否| Initialization\n        ManualBranch -->|SB3: 电磁铁动作| Step2\n        ManualBranch -->|SB4: 向下| Step1\n        ManualBranch -->|SB5: 上升| Step3\n        ManualBranch -->|SB6: 右行| Step4\n        ManualBranch -->|SB7: 左行| Step8\n    end\n
    Step5 --> Delay3[延时模块：1秒]\n    Delay3 --> Step6[释放球]\n    Step6 --> Delay4[延时模块：1秒]\n    Delay4 --> Step7[机械手上升]\n    Step7 --> Step8[左行]\n    Step8 --> CheckCycle{循环\\n还是单周期?}\n\n    CheckCycle -->|循环模式| Step1\n    CheckCycle -->|单周期完成| Stop\n
